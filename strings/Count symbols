s = input()
count1 = 0
count2 = 0

for elem in s:
    if elem == '+':
        count1 += 1
    if elem == '*':
        count2 += 1

print('Символ + встречается', count1, 'раз')
print('Символ * встречается', count2, 'раз')
